export interface Recipe {
  cook_time: string;
  cuisine: string;
  diet: string;
  ingredients: string;
  instruction: string;
  prep_time: string;
  recipeName: string;
  url: string;
  imageUrl?: string;
}